from django.apps import AppConfig


class AboutConfig(AppConfig):
    """
    Configuration for the 'about' Django application.
    """
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'about'
